# Global OpenCode Rules

- If a request is about setting up OpenCode (config, commands, skills, AGENTS, or dotfile linking), load the `opencode-setup` skill first.
- Use repository mapping context from `mapping.toml` when describing where files should live.
